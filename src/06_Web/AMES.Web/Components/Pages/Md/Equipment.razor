@page "/md/equipment"
@rendermode InteractiveServer
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@inject AMES.Data.Repositories.MasterDataRepository Md
@inject AuthenticationStateProvider Auth
@inject IStringLocalizer<SharedResources> L

<PageTitle>@L["MD.Equipment.Title"] · AMES</PageTitle>

<div class="ames-page-h">
    <div class="ames-page-id">MD-014</div>
    <div class="ames-page-t">@L["MD.Equipment.Title"]</div>
    <div class="ames-page-sub">@L["MD.Equipment.Sub"]</div>
</div>

<div class="ames-kpis">
    <div class="ames-kpi">
        <div class="ames-kpi-l">@L["Lbl.Total"]</div>
        <div class="ames-kpi-v">@_allRows.Count</div>
    </div>
    <div class="ames-kpi ok">
        <div class="ames-kpi-l">@L["Lbl.Active"]</div>
        <div class="ames-kpi-v">@_allRows.Count(r => r.ActiveFlag)</div>
    </div>
    <div class="ames-kpi warn">
        <div class="ames-kpi-l">@L["Kpi.LineCount"]</div>
        <div class="ames-kpi-v">@_allRows.Select(r => r.LineID).Where(l => l != null).Distinct().Count()</div>
    </div>
    <div class="ames-kpi">
        <div class="ames-kpi-l">@L["MD.Equipment.Kpi.EquipTypes"]</div>
        <div class="ames-kpi-v">@_allRows.Select(r => r.EquipType).Where(t => t != null).Distinct().Count()</div>
    </div>
</div>

@if (!string.IsNullOrEmpty(_msg))
{
    <div class="ames-alert @(_isErr ? "bad" : "ok")">@_msg</div>
}

<div class="ames-card">
    <div class="ames-card-h" style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap;">
        <span style="white-space:nowrap;">▼ @L["MD.Equipment.List"] · @_rows.Count @L["Lbl.Items"]@if (_rows.Count < _allRows.Count) { <text> / @L["Lbl.TotalOf"] @_allRows.Count</text> }</span>
        <div style="display:flex;gap:6px;align-items:center;flex-wrap:wrap;">
            <input class="ames-input" style="width:160px;padding:4px 8px;"
                   placeholder='@L["MD.Equipment.Ph.Search"]' @bind="_fSearch" @bind:event="oninput" @onkeydown='e => { if (e.Key == "Enter") ApplyFilter(); }' autocomplete="off" />
            <span class="dim" style="font-size:.82em;white-space:nowrap;">@L["Col.Line"]</span>
            <select class="ames-input" style="width:130px;padding:4px 8px;" @bind="_fLine">
                <option value="">@L["Filter.AllLines"]</option>
                @foreach (var l in _lineList)
                {
                    <option value="@l.LineID">@l.LineID@(l.LineName != null ? $" {l.LineName}" : "")</option>
                }
            </select>
            <span class="dim" style="font-size:.82em;white-space:nowrap;">@L["Col.Type"]</span>
            <select class="ames-input" style="width:120px;padding:4px 8px;" @bind="_fType">
                <option value="">@L["Filter.AllTypes"]</option>
                @foreach (var t in _allRows.Select(r => r.EquipType).Where(x => x != null).Distinct().OrderBy(x => x))
                {
                    <option value="@t">@t</option>
                }
            </select>
            <span class="dim" style="font-size:.82em;white-space:nowrap;">@L["Col.Active"]</span>
            <select class="ames-input" style="width:100px;padding:4px 8px;" @bind="_fActive">
                <option value="">@L["Filter.All"]</option>
                <option value="Y">@L["Lbl.Active"]</option>
                <option value="N">@L["Lbl.Inactive"]</option>
            </select>
            <button class="ames-btn ok" @onclick="ApplyFilter">@L["Btn.Search"]</button>
            <button class="ames-btn" @onclick="OpenCreate">@L["Btn.Create"]</button>
        </div>
    </div>

    @if (_rows.Count == 0)
    {
        <div class="ames-empty">@L["MD.Equipment.Empty"]</div>
    }
    else
    {
        <table class="ames-table">
            <thead>
                <tr>
                    <th>@L["Col.EquipId"]</th><th>@L["Col.EquipName"]</th><th>@L["Col.EquipType"]</th><th>@L["Col.Line"]</th><th>@L["Col.WorkCenter"]</th>
                    <th>@L["Col.MakerModel"]</th><th>@L["Col.InstallDate"]</th>
                    <th style="text-align:right;">@L["Col.TheoreticalCycle"]</th>
                    <th style="text-align:right;">@L["Col.TargetOee"]</th>
                    <th>@L["Col.PlcAddr"]</th><th>@L["Col.Status"]</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var r in _rows.Skip(_page * _pageSize).Take(_pageSize))
                {
                    <tr class="ames-row-link" @onclick="() => OpenEdit(r)">
                        <td class="code">@r.EquipID</td>
                        <td>@(r.EquipName ?? "—")</td>
                        <td><span class="pill">@(r.EquipType ?? "—")</span></td>
                        <td class="dim">@(r.LineID ?? "—")</td>
                        <td class="dim">@(r.WCID ?? "—")</td>
                        <td class="dim">@(r.MakerModel ?? "—")</td>
                        <td class="dim">@(r.InstallDate?.ToString("yyyy-MM-dd") ?? "—")</td>
                        <td style="text-align:right;">@(r.TheoreticalCycle?.ToString("F2") ?? "—")</td>
                        <td style="text-align:right;">@(r.TargetOEE?.ToString("F1") ?? "—")</td>
                        <td class="dim">@(r.PlcAddress ?? "—")</td>
                        <td>
                            @{
                                var scls   = r.Status switch { "ACTIVE" => "ok", "MAINT" => "warn", _ => "bad" };
                                var sLabel = _equipStatus.FirstOrDefault(s => s.CodeValue == r.Status) is { } _lf_ ? MasterDataRepository.LocalName(_lf_) : r.Status;
                            }
                            <span class="pill @scls">@(sLabel ?? "—")</span>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <Pager Page="_page" Total="_rows.Count" PageSize="_pageSize" OnPageChange="p => _page = p" />
    }
</div>

@* ── Create Modal ─────────────────────────────────────────────────── *@
@if (_modal == "create")
{
    <div class="ames-modal-bg" @onmousedown="OnBgMouseDown" @onclick="OnBgClick">
        <div class="ames-modal" @onmousedown:stopPropagation @onclick:stopPropagation>
            <div class="ames-modal-h">@L["MD.Equipment.Title"] — @L["Btn.Create"]</div>

            <label>@L["Col.EquipId"] * <span style="font-weight:400;color:#64748b;font-size:.85em;">(예: EQ-INJ-001)</span></label>
            <input class="ames-input" @bind="_f.EquipID" placeholder="EQ-INJ-001" autocomplete="off" />

            <label>@L["Col.EquipName"] *</label>
            <input class="ames-input" @bind="_f.EquipName" placeholder='@L["MD.Equipment.Ph.EquipName"]' autocomplete="off" />

            <label>@L["Col.EquipType"]</label>
            <input class="ames-input" @bind="_f.EquipType" placeholder="INJECTION, PAINT 등" maxlength="16" autocomplete="off" />

            <label>@L["Col.LineId"]</label>
            <select class="ames-input" @bind="_f.LineID">
                <option value="">@L["Opt.Select"]</option>
                @foreach (var l in _lineList)
                {
                    <option value="@l.LineID">[@l.LineID]@(l.LineName != null ? $" {l.LineName}" : "")</option>
                }
            </select>

            <label>Work Center ID</label>
            <select class="ames-input" @bind="_f.WCID">
                <option value="">@L["Opt.Select"]</option>
                @foreach (var w in _wcList.Where(w => w.ActiveFlag))
                {
                    <option value="@w.WCID">[@w.WCID]@(w.WCName != null ? $" {w.WCName}" : "")</option>
                }
            </select>

            <label>@L["Col.MakerModel"]</label>
            <input class="ames-input" @bind="_f.MakerModel" placeholder='@L["MD.Equipment.Ph.MakerModel"]' autocomplete="off" />

            <label>@L["Col.InstallDate"]</label>
            <input class="ames-input" type="date" @bind="_f.InstallDate" autocomplete="off" />

            <label>@L["Col.TheoreticalCycle"]</label>
            <input class="ames-input" type="number" step="0.01" @bind="_f.TheoreticalCycle" placeholder="30.00" min="0" autocomplete="off" />

            <label>@L["Col.TargetOee"]</label>
            <input class="ames-input" type="number" step="0.1" @bind="_f.TargetOEE" placeholder="85.0" min="0" max="100" autocomplete="off" />

            <label>@L["Col.PlcAddr"]</label>
            <input class="ames-input" @bind="_f.PlcAddress" placeholder="192.168.1.10:502" maxlength="40" autocomplete="off" />

            <label>@L["Col.Status"]</label>
            <select class="ames-input" @bind="_f.Status">
                <option value="">@L["Opt.Select"]</option>
                @foreach (var s in _equipStatus.Where(s => s.UseFlag))
                {
                    <option value="@s.CodeValue">@(MasterDataRepository.LocalName(s))</option>
                }
            </select>

            <label style="display:flex;align-items:center;gap:8px;cursor:pointer;">
                <input type="checkbox" @bind="_f.ActiveFlag" />
                <span>@L["Lbl.Active"]</span>
            </label>

            @if (!string.IsNullOrEmpty(_modalErr))
            { <div class="ames-alert bad">@_modalErr</div> }

            <div class="ames-modal-footer">
                <button class="ames-btn" @onclick="SaveCreate" disabled="@_busy">@(_busy ? @L["Msg.Saving"] : @L["Btn.Create"])</button>
                <button class="ames-btn ghost" @onclick="CloseModal">@L["Btn.Cancel"]</button>
            </div>
        </div>
    </div>
}

@* ── Edit Modal ───────────────────────────────────────────────────── *@
@if (_modal == "edit" && _sel is not null)
{
    <div class="ames-modal-bg" @onmousedown="OnBgMouseDown" @onclick="OnBgClick">
        <div class="ames-modal" @onmousedown:stopPropagation @onclick:stopPropagation>
            <div class="ames-modal-h">@L["MD.Equipment.Title"] — @L["Btn.Edit"] <span class="dim" style="font-weight:400;font-size:.9em;">@_sel.EquipID</span></div>

            <label>@L["Col.EquipName"] *</label>
            <input class="ames-input" @bind="_f.EquipName" autocomplete="off" />

            <label>@L["Col.EquipType"]</label>
            <input class="ames-input" @bind="_f.EquipType" maxlength="16" autocomplete="off" />

            <label>@L["Col.LineId"]</label>
            <select class="ames-input" @bind="_f.LineID">
                <option value="">@L["Opt.Select"]</option>
                @foreach (var l in _lineList)
                {
                    <option value="@l.LineID">[@l.LineID]@(l.LineName != null ? $" {l.LineName}" : "")</option>
                }
            </select>

            <label>Work Center ID</label>
            <select class="ames-input" @bind="_f.WCID">
                <option value="">@L["Opt.Select"]</option>
                @foreach (var w in _wcList.Where(w => w.ActiveFlag))
                {
                    <option value="@w.WCID">[@w.WCID]@(w.WCName != null ? $" {w.WCName}" : "")</option>
                }
            </select>

            <label>@L["Col.MakerModel"]</label>
            <input class="ames-input" @bind="_f.MakerModel" autocomplete="off" />

            <label>@L["Col.InstallDate"]</label>
            <input class="ames-input" type="date" @bind="_f.InstallDate" autocomplete="off" />

            <label>@L["Col.TheoreticalCycle"]</label>
            <input class="ames-input" type="number" step="0.01" @bind="_f.TheoreticalCycle" min="0" autocomplete="off" />

            <label>@L["Col.TargetOee"]</label>
            <input class="ames-input" type="number" step="0.1" @bind="_f.TargetOEE" min="0" max="100" autocomplete="off" />

            <label>@L["Col.PlcAddr"]</label>
            <input class="ames-input" @bind="_f.PlcAddress" maxlength="40" autocomplete="off" />

            <label>@L["Col.Status"]</label>
            <select class="ames-input" @bind="_f.Status">
                <option value="">@L["Opt.Select"]</option>
                @foreach (var s in _equipStatus.Where(s => s.UseFlag))
                {
                    <option value="@s.CodeValue">@(MasterDataRepository.LocalName(s))</option>
                }
            </select>

            <label style="display:flex;align-items:center;gap:8px;cursor:pointer;">
                <input type="checkbox" @bind="_f.ActiveFlag" />
                <span>@L["Lbl.Active"]</span>
            </label>

            @if (_confirmDelete)
            {
                <div class="ames-delete-confirm">
                    <span>@L["Msg.Confirm.Delete"]</span>
                    <button class="ames-btn ghost" @onclick='() => _confirmDelete = false'>@L["Msg.No"]</button>
                    <button class="ames-btn bad" @onclick="SaveDelete" disabled="@_busy">@(_busy ? @L["Msg.Deleting"] : @L["Msg.Yes"])</button>
                </div>
            }

            @if (!string.IsNullOrEmpty(_modalErr))
            { <div class="ames-alert bad">@_modalErr</div> }

            <div class="ames-modal-footer">
                <button class="ames-btn"  @onclick="SaveEdit"   disabled="@_busy">@(_busy ? @L["Msg.Saving"] : @L["Btn.Edit"])</button>
                <button class="ames-btn bad" @onclick='() => { _confirmDelete = true; _modalErr = ""; }' disabled="@_busy">@L["Btn.Delete"]</button>
                <button class="ames-btn ghost"     @onclick="CloseModal">@L["Btn.Cancel"]</button>
            </div>
        </div>
    </div>
}

@code {
    List<AMES.Data.Repositories.MasterDataRepository.EquipmentRow> _allRows = new();
    List<AMES.Data.Repositories.MasterDataRepository.EquipmentRow> _rows    = new();
    AMES.Data.Repositories.MasterDataRepository.EquipmentRow? _sel;
    int _page = 0;
    const int _pageSize = 20;

    List<AMES.Data.Repositories.MasterDataRepository.LineRow>       _lineList    = new();
    List<AMES.Data.Repositories.MasterDataRepository.WorkCenterRow> _wcList      = new();
    List<AMES.Data.Repositories.MasterDataRepository.CodeItemRow>   _equipStatus = new();
    string _fSearch = "", _fLine = "", _fType = "", _fActive = "";

    string _modal = "";
    bool _busy, _confirmDelete, _bgMouseDownOnSelf;
    string _msg = "", _modalErr = "";
    bool _isErr;
    string _actor = "system";
    FormModel _f = new();

    protected override async Task OnInitializedAsync()
    {
        var auth = await Auth.GetAuthenticationStateAsync();
        _actor = auth.User.Identity?.Name ?? "system";
        try { _lineList    = Md.ListLines();                        } catch { }
        try { _wcList      = Md.ListWorkCenters();                  } catch { }
        try { _equipStatus = Md.ListCodeItems("EQUIP_STATUS"); } catch { }
        Reload();
    }

    void Reload() { _allRows = Md.ListEquipments(); ApplyFilter(); }

    void ApplyFilter() { _page = 0; _rows = _allRows.Where(r =>
        (string.IsNullOrEmpty(_fSearch) ||
            r.EquipID.Contains(_fSearch, StringComparison.OrdinalIgnoreCase) ||
            (r.EquipName ?? "").Contains(_fSearch, StringComparison.OrdinalIgnoreCase)) &&
        (string.IsNullOrEmpty(_fLine)   || r.LineID    == _fLine) &&
        (string.IsNullOrEmpty(_fType)   || r.EquipType == _fType) &&
        (string.IsNullOrEmpty(_fActive) || (_fActive == "Y" ? r.ActiveFlag : !r.ActiveFlag))
    ).ToList(); }

    void OpenCreate() { _f = new() { ActiveFlag = true, Status = "Active" }; _modalErr = ""; _confirmDelete = false; _modal = "create"; }

    void OpenEdit(AMES.Data.Repositories.MasterDataRepository.EquipmentRow r)
    {
        _sel = r; _modalErr = ""; _confirmDelete = false;
        _f = new()
        {
            EquipID          = r.EquipID,
            EquipName        = r.EquipName    ?? "",
            EquipType        = r.EquipType    ?? "",
            LineID           = r.LineID       ?? "",
            WCID             = r.WCID         ?? "",
            MakerModel       = r.MakerModel   ?? "",
            InstallDate      = r.InstallDate,
            TheoreticalCycle = r.TheoreticalCycle,
            TargetOEE        = r.TargetOEE,
            PlcAddress       = r.PlcAddress   ?? "",
            Status           = r.Status       ?? "Active",
            ActiveFlag       = r.ActiveFlag,
        };
        _modal = "edit";
    }

    void CloseModal() { _modal = ""; _sel = null; _modalErr = ""; _confirmDelete = false; }

    void OnBgMouseDown() => _bgMouseDownOnSelf = true;
    void OnBgClick() { if (_bgMouseDownOnSelf) CloseModal(); _bgMouseDownOnSelf = false; }

    static string? NE(string? s) => string.IsNullOrWhiteSpace(s) ? null : s;

    void SaveCreate()
    {
        _modalErr = "";
        if (string.IsNullOrWhiteSpace(_f.EquipID))   { _modalErr = L["Err.EquipIdRequired"]; return; }
        if (string.IsNullOrWhiteSpace(_f.EquipName))  { _modalErr = L["Err.EquipNameRequired"]; return; }
        if (Md.EquipmentExists(_f.EquipID.Trim()))    { _modalErr = $"설비ID '{_f.EquipID.Trim()}' 이 {L["Err.AlreadyExists"]}"; return; }
        _busy = true;
        try
        {
            Md.InsertEquipment(_f.EquipID.Trim(), _f.EquipName, NE(_f.EquipType),
                NE(_f.LineID), NE(_f.WCID), NE(_f.MakerModel),
                _f.InstallDate, _f.TheoreticalCycle, _f.TargetOEE,
                NE(_f.PlcAddress), NE(_f.Status), _f.ActiveFlag, _actor);
            CloseModal(); Reload();
            ShowMsg($"{_f.EquipName} {L["Msg.Created"]}", false);
        }
        catch (Exception ex) { _modalErr = ex.Message; }
        finally { _busy = false; }
    }

    void SaveEdit()
    {
        if (_sel is null) return;
        _modalErr = "";
        if (string.IsNullOrWhiteSpace(_f.EquipName)) { _modalErr = L["Err.EquipNameRequired"]; return; }
        _busy = true;
        try
        {
            Md.UpdateEquipment(_sel.EquipID, _f.EquipName, NE(_f.EquipType),
                NE(_f.LineID), NE(_f.WCID), NE(_f.MakerModel),
                _f.InstallDate, _f.TheoreticalCycle, _f.TargetOEE,
                NE(_f.PlcAddress), NE(_f.Status), _f.ActiveFlag, _actor);
            var name = _f.EquipName;
            CloseModal(); Reload();
            ShowMsg($"{name} {L["Msg.Updated"]}", false);
        }
        catch (Exception ex) { _modalErr = ex.Message; }
        finally { _busy = false; }
    }

    void SaveDelete()
    {
        if (_sel is null) return;
        _busy = true;
        try
        {
            var name = _sel.EquipName ?? _sel.EquipID;
            Md.DeleteEquipment(_sel.EquipID);
            CloseModal(); Reload();
            ShowMsg($"{name} {L["Msg.Deleted"]}", false);
        }
        catch (Exception ex) { _modalErr = ex.Message; _confirmDelete = false; }
        finally { _busy = false; }
    }

    void ShowMsg(string msg, bool isErr)
    {
        _msg = msg; _isErr = isErr;
        _ = Task.Delay(4000).ContinueWith(_ => { _msg = ""; InvokeAsync(StateHasChanged); });
    }

    class FormModel
    {
        public string   EquipID          { get; set; } = "";
        public string   EquipName        { get; set; } = "";
        public string   EquipType        { get; set; } = "";
        public string   LineID           { get; set; } = "";
        public string   WCID             { get; set; } = "";
        public string   MakerModel       { get; set; } = "";
        public DateOnly? InstallDate     { get; set; }
        public decimal? TheoreticalCycle { get; set; }
        public decimal? TargetOEE        { get; set; }
        public string   PlcAddress       { get; set; } = "";
        public string   Status           { get; set; } = "Active";
        public bool     ActiveFlag       { get; set; } = true;
    }
}
